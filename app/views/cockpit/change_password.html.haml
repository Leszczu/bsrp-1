-title "Zmiana hasła"%h1 Zmiana hasła-form_for @player, :url => update_password_path do |f|  Obecne hasło  %p    = f.password_field :password    %br    Wpisz nowe hasło    %br    = f.password_field :new_password    %br    Wpisz ponownie nowe hasło    %br    = f.password_field :new_password_confirmation  = f.submit "Gotowe"